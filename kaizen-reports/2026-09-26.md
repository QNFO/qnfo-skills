# Kaizen Report — 2026-09-26 (CMD UPDATE HOLISTIC, wave-2)

## Cycle scope
CMD UPDATE (HOLISTIC) executed against a fleet already mid-cycle: a concurrent wave-1 session
had completed the v4.39->v4.40 / kaizen v2.155->v2.156 bump and committed it (qnfo-skills 00b0094:
AI-GW-COST-UNIT-CENTS-1 + CRON-RECONCILE-CONVERGE-1 + SCHEDULES-PUT-ARRAY-1 +
REGISTRY-VERSION-LIVE-PRIMARY-1 + DEPLOY-LOCK-EPOCH-TYPE-1). This wave-2 converged additively
(same v4.40, no version fight) with the post-publication red-team lessons.

## Cycle work
1. **Post-publication red-team audit** of adelic-core-synthesis (10.5281/zenodo.21786473):
   verdict FAIL — embedded LLM transcripts (34x DeepSeek, 8x ChatGPT) live on papers.qnfo.org,
   stale frontmatter (doi:TBD / status:draft), no PDF anywhere, r2_key null, KG node isolated
   (0 edges), Zenodo v1 only, and the prior "fully remediated" claim had no ledger rows
   (handoffs 29422).
2. **Same-turn data-layer remediation** (handoff 29425): frontmatter synced
   (cc-by-4.0 + real DOI + published), bodyless duplicate row deleted (FTS-trigger-consistent),
   R2 .md deposited + r2_key set, 6 sibling KG Paper nodes created + 8 neutral RELATES_TO edges
   (no fabricated CITES). Content/publish items deferred to v1.1 with owner+due
   (task_dod_register 328/329, due 2026-10-03).
3. **Wave-2 gates enshrined** (system-prompt v4.40 wave-2 + kaizen v2.156 wave-2):
   EMBEDDED-TRANSCRIPT-SWEEP-1, AUDIT-LEDGER-ONLY-TRUST-1, TASK-DOD-REGISTER-ONE-ROW-1,
   KG-EDGE-NO-FABRICATED-CITES-1, FTS-EXTERNAL-CONTENT-DELETE-1. Additive dual-write across
   3 prompt md copies + 2 kaizen copies + history; live stores propagated via
   sync_system_prompt.py.

## Lessons (evidence-backed)
1. **Concurrent-session convergence works but leaves duplicates.** A parallel instance with the
   same conversation context applied identical wave-2 edits (banner/history/version) while this
   session applied lastupdated/gates-block/kaizen — the file ended with TWO identical gates
   blocks and TWO kaizen wave-2 banners/logs. Idempotent patching + a post-write
   duplicate-sweep (marker line counts) are mandatory when sessions share a conversation.
2. **The Last-updated header line lags bumps.** Pre-flight: title v4.39 while "Last updated"
   described v4.37 (two bumps behind). PROMPT-HEADER-LAST-UPDATED-DRIFT-1: title + banner +
   Last-updated + Version "Current" must all move together.
3. **A published body must be transcript-swept at publish.** ds_n/gpt_n token probes catch the
   raw-chat leak that shipped live for 7 weeks.
4. **Audit verdicts are trustworthy only as ledger rows.** The v2 "zero blocking findings /
   fully remediated" memory did not survive live re-probing and left no handoffs/wbs rows.
5. **Registry state vs live-code check**: qnfo-email-orchestrator row says state=retired with
   version "0.4.2-retired-health-stub-route-repair" — its live code carries that exact VERSION;
   a documented transitional stub, NOT drift (live census 31 workers == 31 registry rows,
   0 ghosts/unregistered/unversioned).

## Guards (all exit 0)
- prompt-store-verify.py: PASS — 6-store system-prompt parity, 17 skill anchors,
  11/11 CMD templates (ADVERSARIAL + DOD-AUDIT + update_plan), MCP-AUTOAPPROVE 9/9,
  gate manifests valid, all adversarial surfaces.
- scheduler-guard.py: PASS — 0 enabled local cron rows.
- model_guard.py: state=clean — desired AI-GATEWAY/openai/gpt-4.1 across all 8 stores
  (auto-fixed 3 session pins + JSON preferredModel earlier in the cycle).

## Open items (owner + due)
- adelic-core-synthesis v1.1 remediation cycle (transcripts/PDF/Vectorize/Zenodo v1.1) —
  task_dod_register 328/329, due 2026-10-03, owner agent (USER escalation for authorial prose).
- Fleet census wording: wave-1 text says "30 workers", this session measured 31 live —
  recorded in the prompt as "live census this session: 31 workers"; reconcile at next scan.

---

## 2026-09-26 — SINGLE-MODEL ENDPOINTS + deploy/registry integrity (session deepchat-2026-09-26-aiep)

**Requirement.** One cost+performance-optimized model per endpoint, routing hidden, universal OpenAI-client compatibility, OPS cost right-sizing.

**Delivered + live-verified:** qnfo-ops `ops` (19→1), qnfo-ai `qnfo` (14→1), personal-api `personal` (5→1); foreign model ids never rejected (UNIVERSAL-OPENAI-MODEL-COMPAT-1); OPS streaming made free-first (cost root cause = `callDeepSeekStream` had no free-first branch → every streamed ops conversation billed the paid route; $102.65/30d single user).

**Prompt gates (v4.44, preserved in v4.45):** ONE-MODEL-PER-ENDPOINT-1 · UNIVERSAL-OPENAI-MODEL-COMPAT-1 · OPS-STREAM-FREE-FIRST-1 · REGISTRY-MODELS-FIELD-STALE-1 · DEPLOY-GUARD-OPTIMISTIC-LEDGER-1 · DEPLOY-GUARD-WIN-NPX-CMD-1 · DO-EXPORTS-STORAGE-VERIFY-1.

**New operational lessons (recorded here; candidate prompt gates for the next genuine cycle):**
- **HOLISTIC-WRITE-CONCURRENCY-GUARD-1:** before any prompt/kaizen write, check the canonical mtime (<60s ⇒ skip), `git status --porcelain`, and a held `backup_deepchat.lock`; apply the atomic assert-count==1-before-write pattern so a diverged base fails closed instead of silently overwriting.
- **BACKUP-LOCK-IS-LIVE-NOT-STALE-1:** a held `backup_deepchat.lock` whose PID is alive is a LIVE concurrent backup — never clear it to force a closeout backup; fail closed. Canonical 2026-09-26: PID 5776 held the lock ~5 min during a multi-GB agent.db backup.
- **TITLE-LINE-PARITY-ON-WRITE-1:** when bumping a skill/prompt version, bump ALL THREE anchors (H1 + frontmatter/banner + footer) in the SAME write. Canonical: a v4.43→v4.44 kaizen edit bumped fm/footer to v2.160 but left H1 at v2.159 → peer-repaired; psv now carries the check (PROMPT-TITLE-FOOTER-DRIFT, commit 53c69ed).

**Verification (same-turn):** psv rc=0 · prompt-parity-guard rc=0 · scheduler-guard rc=0 · model_guard clean · adversarial-guard rc=0 · fleet 30==30 (registry drift 0) · MCP file==DB parity PASS · AI Gateway monthly-200.

---

## 2026-09-26 — CMD TEMPLATE DE-ACCRETION + TEMPLATE-PARAM-PARITY-1 (session Q4sKQDk-po0llxl1e0Inj)

**Requirement.** AUDIT AND REWRITE ALL CUSTOM PROMPT TEMPLATES to remediate root causes + failure modes; then RED-TEAM + EXECUTE ALL REMEDIATION; then the full HOLISTIC cycle.

**Root causes remediated (all 11 CMD templates; commit 40190bc):**
- **F1 undeclared parameters** — FIND PAPERS ON TOPIC `{{topic}}` + VALIDATE CITATIONS `{{file}}` declared `parameters:[]`; the app's fill loop (`fill_prompt_template`, app.asar) iterates ONLY declared parameters, so the placeholders rendered literally. Declared both.
- **F2 append-only accretion** — CMD UPDATE (HOLISTIC) was 48 KB of dated blocks self-contradicting (7/7 vs 10/10 vs 11/11 vs 12/12 templates; deepseek-v4-flash vs AI-GATEWAY/openai/gpt-4.1; 51/54/78 workers; $90 vs $150). Collapsed to 4.6 KB, delegating to the system prompt + research/SKILL.md as the canonical gate registries.
- **F3 hardcoded volatile facts** -> "read live"; **F4** stale shared CONVENTIONS block -> refreshed; **F5** conflicting CMD DEPLOY preambles -> single server-side /ops/deploy path; **F6** uniform int timestamps, template==content.
- **RESTORE-CP-FILE-GAP-1 recurrence** — restore_custom_prompts.py now writes the deepchat_skills_canon store that prompt-store-verify.py drift-sweeps.

**RED-TEAM (direct in-session, 5-adversary) — a real regression the rewrite introduced:**
- **ORPHAN-GATE-DETECTOR-1** (new method): extract every gate-ID from the OLD templates and assert each survives in the NEW templates OR the canonical authority. 209 old tokens -> 169 correctly delegated, 9 orphans. 8 restored (7 still-valid gates that existed only in templates -> CMD PUBLISH: ZENODO-BUCKET-PUT-415-1, ZENODO-DEPOSIT-FILE-DOWNLOAD-1, ZENODO-DELETE-COUNT-VERIFY-1, ZENODO-RECORDS-PIDS-ON-DEPOSIT-DRAFT-1, ZENODO-ACCESS-RIGHT-LEGACY-1, INDEXER-HOST-1, KG-SYNC-401-FALLBACK-1; + RED-TEAM-AUDIT-METHOD-1 -> CMD RED TEAM); 1 intentional (stale census snapshot). Also removed a self-inflicted hardcoded "v4.46". (commit 9f87753)
- **TEMPLATE-PARAM-PARITY-1** (new FATAL gate; commit 9e6251d + qnfo-ops 521090a): `check_template_parameters()` in prompt-store-verify.py — a `{{placeholder}}` with no declared parameter is a non-functional fill template that passes every schema check. Negative-tested. Now enshrined in system-prompt v4.47 + kaizen v2.163 (converged with the concurrent wave).

**Concurrency incident + repair (HOLISTIC-WRITE-CONCURRENCY-GUARD-1 fired):** a parallel session bumped the system prompt to v4.47 (adding PARITY-GUARD-WIRED-FATAL-1 + FAIL-CLOSED-ANCHOR-1 + GUARD-MIRROR-PARITY-1 + TEMPLATE-PARAM-PARITY-1) at 13:27:41 and left the live stores at v4.46 (psv SYSPROMPT-DRIFT, rc=1). This session did NOT clobber — the atomic assert-anchors-before-write pattern failed closed. After the writer froze >2 min, it REPAIRED idempotently: propagated canonical v4.47 to all 6 stores via sync_system_prompt.py + the skills copy. Duplicate-sweep: clean (1 header each). The parallel session's `system-prompt-v2.7.md` + `kaizen/SKILL.md` remain UNCOMMITTED (GIT-OWNERSHIP-1 — another session's files).

**Verification (same-turn):** psv rc=0 (6-store system-prompt parity, 17 skill anchors, TEMPLATE-PARAM-PARITY PASS, 11/11 CMD templates) · scheduler-guard rc=0 · model_guard rc=0 · adversarial-guard rc=0 · fleet drift 0 (service_registry 33 == live CF scripts 33; fleet_drift_report drifted=0) · MCP 9 servers / 6 enabled · AI Gateway spend_limit monthly-200.

**RED-TEAM skills finding (open, owner):** the accretion disease is FLEET-WIDE in skills — kaizen 255 banners, research 79, cloudflare 48, qnfo-core 43, email-composer 29; anchors are aligned (psv SKILL-ANCHOR-PARITY PASS). De-accreting the large skills is a dedicated multi-session project (owner: next dedicated skills-de-accretion cycle), not a single-cycle task.
